# AGEnt
AGEnt is an experimental age verification software that uses IQ to estimate and predict the user's age group. It uses a total score from an IQ test, a survey score, and some IQ samples to find close matches. This is an open-source.
